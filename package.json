{
  "name": "purescript-base",
  "version": "1.0.0",
  "description": "A base configuration for a PureScript project.",
  "scripts": {
    "container": "docker run -it -v ${PWD}:/app purescript-starter-project:dev",
    "build": "docker build -t purescript-starter-project:dev .",
    "repl": "npm run container spago repl",
    "spago-install": "npm run container spago install",
    "test": "npm run container spago test"
  },
  "author": "@JEngdahl",
  "license": "ISC"
}